# Architectural Decisions

Add an ADR only when implementation requires a decision that affects public contracts, package boundaries, framework strategy, compatibility, or long-term maintenance.

Do not create speculative ADRs for every open question in `PROJECT-PRD.md`.
