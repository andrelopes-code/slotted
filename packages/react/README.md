# React Package Anchor

This directory is reserved for Slotted's future idiomatic React implementation.

It is not a package yet. Do not add a `package.json`, public exports, components, or build tooling until package boundaries and the React build strategy are approved through a dedicated ADR.
