# Angular Package Anchor

This directory is reserved for Slotted's future idiomatic Angular implementation.

It is not a package yet. Do not add a `package.json`, public exports, components, or build tooling until package boundaries and the Angular build strategy are approved through a dedicated ADR.
